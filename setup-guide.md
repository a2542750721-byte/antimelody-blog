# 项目自定义改进指南 (Setup Guide)

> **最后更新**: 2026-01-19  
> **用途**: 记录当前项目所有可自定义的元素和改进空间，供未来参考和迭代

---

## 📋 目录

- [配置文件](#配置文件)
- [样式系统](#样式系统)
- [JavaScript 功能](#javascript-功能)
- [可借鉴的 HeoWeb 设计元素](#可借鉴的-heoweb-设计元素)
- [待优化清单](#待优化清单)

---

## 配置文件

### `_config.butterfly.yml` (主题配置)

#### 1. 视觉定制
```yaml
# 首页背景图
index_img: http://youke.xn--y7xa690gmna.cn/s1/2026/01/19/696d14f352e39.jpg

# 深浅模式 (light, dark, auto)
display_mode: dark

# 网站背景色
background: "#000000"
```

**改进方向**:
- [ ] 切换为自建图床或 CDN (避免外链失效)
- [ ] 添加响应式背景图 (移动端 / PC 端不同)
- [ ] 实现主题色动态切换功能

#### 2. 导航菜单
```yaml
menu:
  首页: / || fa-solid fa-home
  归档: /archives/ || fa-solid fa-archive
  清单:
    音乐: /music/ || fa-solid fa-music
    电影: /movies/ || fa-solid fa-video
```

**改进方向**:
- [ ] 为二级菜单添加"下拉动画"效果
- [ ] 图标换成自定义 SVG 以提升视觉一致性
- [ ] 添加"阅读时长"或"最近更新"等动态菜单项

#### 3. 侧边栏卡片
```yaml
aside:
  card_author:
    description: 分享设计与科技生活
    avatar: http://youke.xn--y7xa690gmna.cn/s1/2026/01/19/696d14f352e39.jpg
    social:
      AI助手: /# || fa-solid fa-robot
      Github: https://github.com/a2542750721-byte || fa-brands fa-github
```

**改进方向**:
- [ ] 添加"时钟问候语"交互 (已实现 `greeting.js`)
- [ ] 社交按钮增加 Tooltip 提示
- [ ] 头像添加"鼠标跟随"视差效果

#### 4. 评论系统
```yaml
giscus:
  repo: # 你的仓库名
  repo_id: 
  category: Announcements
```

**改进方向**:
- [ ] 配置 Giscus 参数 (需先创建 GitHub Discussions)
- [ ] 添加"评论预加载"功能提升性能
- [ ] 实现匿名评论功能 (Waline / Twikoo)

#### 5. CDN 与性能
```yaml
inject:
  head:
    - <link rel="stylesheet" href="/css/custom.css">
  bottom:
    - <script src="https://instant.page/5.2.0" type="module"></script>
```

**改进方向**:
- [ ] 启用 Vercel Edge Functions 做预加载
- [ ] 将 instant.page 改为自托管版本
- [ ] 添加 Service Worker 实现离线访问

---

## 样式系统

### `source/css/custom.css` (核心样式)

#### 1. CSS 变量体系
```css
:root {
    --heo-theme: #425AEF;            /* 主题色 */
    --heo-theme-op: rgba(66, 90, 239, 0.14);
    --heo-shadow-glow: 0 0 20px rgba(66, 90, 239, 0.3);
    --style-radius: 12px;            /* 全局圆角 */
}
```

**改进方向**:
- [ ] 支持"调色盘切换" (预设 3 种配色方案)
- [ ] 添加 `--font-family-code` 变量统一管理代码字体
- [ ] 增加 `--spacing-unit` 统一间距系统

#### 2. 导航栏动效
```css
/* 胶囊式动态导航栏 */
#nav.nav-fixed {
    width: 600px !important;
    border-radius: 50px !important;
    backdrop-filter: blur(20px) saturate(180%) !important;
}
```

**改进方向**:
- [ ] 添加"滚动方向检测"，向下滚动时隐藏导航栏
- [ ] Logo 缩小时显示网站 Favicon
- [ ] 菜单项 Hover 时添加"下划线动画"

#### 3. 侧边栏卡片
```css
#aside-content .card-widget.card-info {
    background: rgba(0, 0, 0, 0.7) !important;
    border: 1px solid rgba(66, 90, 239, 0.3) !important;
}
```

**改进方向**:
- [ ] 添加"粒子背景"动画效果
- [ ] 头像旋转改为"鼠标跟随 3D 倾斜"
- [ ] 社交按钮添加"波纹点击"效果

#### 4. 卡片悬停效果
```css
.recent-posts > .recent-post-item:hover {
    transform: translateY(-5px);
    box-shadow: var(--heo-shadow-theme), var(--heo-shadow-glow) !important;
}
```

**改进方向**:
- [ ] 添加"鼠标进入方向感知"动画
- [ ] 悬停时显示"阅读时长"标签
- [ ] 配合背景图实现"视差偏移"

---

## JavaScript 功能

### `source/js/` (交互脚本)

#### 1. `greeting.js` - 问候语系统
```javascript
// 根据时间显示不同问候语
if (hour >= 5 && hour < 11) welcome_text = '早上好，一日之计在于晨！';
```

**改进方向**:
- [ ] 添加"天气 API"显示当前天气
- [ ] 节假日自动切换特殊问候语
- [ ] 添加"一言"随机句子功能

#### 2. `nav-dynamic.js` - 导航栏动态收缩
```javascript
// 监听滚动，动态添加/移除 .nav-fixed 类
```

**改进方向**:
- [ ] 优化为"节流函数"，减少重绘次数
- [ ] 添加"滚动进度条"显示阅读位置
- [ ] 实现"点击返回顶部"平滑动画

#### 3. `border-effect.js` - 边框特效
**改进方向**:
- [ ] 添加"鼠标跟随粒子"效果
- [ ] 实现"光标磁吸"效果 (按钮吸附)
- [ ] 添加"打字机"标题效果

#### 4. `rightmenu.js` - 右键菜单
**改进方向**:
- [ ] 添加"复制代码块"快捷键
- [ ] 右键菜单支持"二维码生成"
- [ ] 添加"划词搜索"功能

---

## 可借鉴的 HeoWeb 设计元素

> 参考文件: `heo_design_reference.md`

### 1. 磨砂玻璃导航栏
```css
.nav-bar {
    backdrop-filter: blur(12px);
    background: rgba(255, 255, 255, 0.8);
}
```

### 2. 悬浮动画 (Hero Section 主图)
```css
@keyframes floating {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}
```

### 3. 胶囊按钮
```css
.pill-btn {
    border-radius: 50px;
    padding: 8px 24px;
    transition: filter 0.3s;
}
.pill-btn:hover {
    filter: brightness(1.2);
}
```

### 4. Banner 入场动画
```css
@keyframes showbanner {
    0% { top: 0; background: var(--heo-theme); }
    100% { top: 40px; background: var(--heo-secondbg); }
}
```

---

## 待优化清单

### 🔥 高优先级
- [ ] **性能优化**: 添加 Webp 图片格式自动转换
- [ ] **SEO 优化**: 完善 Open Graph 和 Twitter Card 元标签
- [ ] **无障碍**: 添加 ARIA 标签，支持键盘导航
- [ ] **深色模式**: 完善深色模式下的色彩对比度

### 📝 中优先级
- [ ] **评论系统**: 配置 Giscus/Waline
- [ ] **搜索功能**: 本地搜索换成 Algolia
- [ ] **统计分析**: 接入 Google Analytics 4
- [ ] **RSS 订阅**: 生成全文 RSS

### 💡 低优先级 (实验性)
- [ ] 添加"音乐播放器"悬浮组件
- [ ] 实现"阅读模式"一键切换
- [ ] 添加"代码高亮主题切换"
- [ ] 集成"即时聊天"功能 (Crisp / Tidio)

---

## 文件结构速查

```
Blogs/
├── _config.yml                    # Hexo 核心配置
├── _config.butterfly.yml          # Butterfly 主题配置 ⭐
├── source/
│   ├── css/
│   │   ├── custom.css            # 自定义样式 ⭐
│   │   └── rightmenu.css         # 右键菜单样式
│   ├── js/
│   │   ├── greeting.js           # 问候语 ⭐
│   │   ├── nav-dynamic.js        # 导航栏动态 ⭐
│   │   ├── border-effect.js      # 边框特效
│   │   └── rightmenu.js          # 右键菜单
│   └── _posts/                   # 文章目录
└── themes/butterfly/             # 主题源文件 (需谨慎修改)
```

---

## 常用命令

```bash
# 本地预览
npm run server

# 清除缓存
hexo clean

# 生成静态文件
hexo generate

# 部署到 Vercel (通过 GitHub Actions)
git push origin main
```

---

## 参考资源

- [Butterfly 官方文档](https://butterfly.js.org/)
- [HeoWeb 设计参考](./heo_design_reference.md)
- [Hexo 插件市场](https://hexo.io/plugins/)
- [Font Awesome 图标库](https://fontawesome.com/icons)

---

**💡 使用建议**: 每次修改前先备份，逐项测试效果，避免一次性修改过多导致难以定位问题。
